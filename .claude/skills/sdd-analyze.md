# /sdd-analyze

레거시 코드베이스를 분석하여 도메인 경계와 스펙 후보를 식별합니다.

## 사용법

```
/sdd-analyze [path]              # 프로젝트 분석
/sdd-analyze --depth deep        # 깊은 분석
/sdd-analyze --suggest-domains   # 도메인 제안
/sdd-analyze --complexity        # 복잡도 분석
```

## 주요 기능

### 기본 분석

프로젝트 구조와 코드를 분석합니다.

```
/sdd-analyze
/sdd-analyze src/
/sdd-analyze --json
```

출력 예시:
```
📊 프로젝트 분석 결과

파일: 120개
심볼: 1,245개
언어: TypeScript (85%), JavaScript (15%)

추정 도메인:
  - auth (높음) - 15개 파일, 89개 심볼
  - core (높음) - 25개 파일, 234개 심볼
  - api (중간) - 10개 파일, 67개 심볼

복잡도: B (양호)
```

### 도메인 경계 식별

코드 구조에서 도메인 경계를 추론합니다.

```
/sdd-analyze --suggest-domains
```

출력 예시:
```
📁 도메인 제안

1. auth (신뢰도: 92%)
   경로: src/auth/
   이유: 디렉토리 이름, 관련 심볼 밀집
   스펙 후보:
     - user-login
     - oauth-google
     - session-management

2. core (신뢰도: 88%)
   경로: src/core/
   이유: 공통 유틸리티 집합
   스펙 후보:
     - data-model
     - validation
```

### 복잡도 분석

코드베이스의 복잡도를 평가합니다.

```
/sdd-analyze --complexity
```

지표:
- **LOC**: 코드 줄 수
- **순환 복잡도**: 분기 복잡도
- **의존성 깊이**: 모듈 간 결합도
- **테스트 커버리지**: 테스트 존재 비율

### 깊은 분석

심볼 수준의 상세 분석을 수행합니다.

```
/sdd-analyze --depth deep
```

Serena MCP가 연결된 경우:
- 클래스/함수 시그니처 추출
- 타입 정보 분석
- 의존성 그래프 생성
- 주석/문서 추출

## 분석 전략

### 디렉토리 기반

```
src/
├── auth/        → auth 도메인
├── core/        → core 도메인
├── api/         → api 도메인
└── utils/       → 공유 유틸 (도메인 아님)
```

### 네이밍 패턴

```
*Controller  → API/라우팅 도메인
*Service     → 비즈니스 로직 도메인
*Repository  → 데이터 접근 도메인
*Utils       → 공유 유틸리티
```

### 의존성 기반

```
core ← auth ← api
       ↑
    payment
```

의존성이 없는 모듈 = 독립 도메인 후보

## 출력 형식

```
/sdd-analyze --format json > analysis.json
/sdd-analyze --format markdown > analysis.md
```

## 다음 단계

분석 후 권장 워크플로우:

1. `/sdd-analyze --suggest-domains` - 도메인 후보 확인
2. `/sdd-domain create <name>` - 도메인 생성
3. `/sdd-reverse scan` - 상세 스캔
4. `/sdd-reverse extract` - 스펙 추출

## 참고사항

- Serena MCP 연결 시 더 정확한 분석 가능
- 대규모 프로젝트는 디렉토리 단위 분석 권장
- 분석 결과는 참고용이며 수동 검토 필요
