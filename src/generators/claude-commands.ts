/**
 * Claude Code 슬래시 커맨드 생성기
 *
 * @deprecated 이 파일은 하위 호환성을 위해 유지됩니다.
 * 새 코드는 './claude-commands/index.js'에서 import하세요.
 */

// 새 모듈에서 모두 재내보내기
export { generateClaudeCommands } from './claude-commands/index.js';
export type { ClaudeCommand } from './claude-commands/index.js';
